import { AlgoResultsRequest } from './algo-results-request';

describe('AlgoResultsRequest', () => {
  it('should create an instance', () => {
    expect(new AlgoResultsRequest()).toBeTruthy();
  });
});
