import { PickSheetDetailsRequest } from './pick-sheet-details-request';

describe('PickSheetDetailsRequest', () => {
  it('should create an instance', () => {
    expect(new PickSheetDetailsRequest()).toBeTruthy();
  });
});
