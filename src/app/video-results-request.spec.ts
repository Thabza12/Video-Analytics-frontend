import { VideoResultsRequest } from './video-results-request';

describe('VideoResultsRequest', () => {
  it('should create an instance', () => {
    expect(new VideoResultsRequest()).toBeTruthy();
  });
});
