import { PickSheetResource } from './pick-sheet-resource';

describe('PickSheetResource', () => {
  it('should create an instance', () => {
    expect(new PickSheetResource()).toBeTruthy();
  });
});
